# Verdict API

## Commands

```bash
cargo check
cargo clippy -- -D warnings
cargo test
```

## Local Postgres

```bash
docker compose up -d
```

Set `DATABASE_URL` from `.env.example` before starting the API.
